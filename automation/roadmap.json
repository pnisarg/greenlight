{
  "schema_version": 1,
  "project": "Greenlight Product",
  "repository": "pnisarg/greenlight",
  "default_branch": "main",
  "merge_strategy": "merge",
  "max_parallel": 1,
  "max_implementation_attempts": 3,
  "max_review_rounds": 3,
  "max_ci_fix_rounds": 2,
  "global_validation": [
    "uv run --extra dev pytest -q",
    "uv run ruff check .",
    "npm test",
    "npm run typecheck"
  ],
  "tasks": [
    {
      "id": "GL-001",
      "phase": "integrity",
      "title": "Fail closed on malformed review verdicts",
      "branch": "fix/strict-review-verdicts",
      "pr_title": "fix(review): validate reviewer verdicts strictly",
      "depends_on": [],
      "intent": "Make reviewer output structurally trustworthy before any UI consumes it. Reject malformed findings and duplicate reviewer identities instead of silently converting them into a clean verdict, while preserving valid existing reviewer configuration.",
      "scope": [
        "Strictly validate every finding field and severity",
        "Treat malformed items as an inconclusive blocking verdict",
        "Require unique reviewer identities in configuration",
        "Add regression tests for malformed and duplicate inputs"
      ],
      "non_goals": ["UI work", "Changing the review prompts beyond schema requirements", "Finding waivers"],
      "acceptance": [
        "A non-object finding fails closed",
        "Unknown severity, empty description, invalid file, or invalid line fails closed",
        "Duplicate reviewer identifiers are rejected at configuration load",
        "Valid reviewer output remains backward compatible"
      ],
      "validation": ["uv run --extra dev pytest -q tests/test_review_fail_closed.py tests/test_review_events.py tests/test_config.py", "uv run ruff check src/greenlight tests"]
    },
    {
      "id": "GL-002",
      "phase": "integrity",
      "title": "Snapshot trusted review policy outside the judged branch",
      "branch": "feat/trusted-policy-snapshots",
      "pr_title": "feat(policy): add trusted policy snapshots",
      "depends_on": ["GL-001"],
      "intent": "Separate authoritative Greenlight policy from code under review. Introduce a local trusted policy snapshot and digest while keeping repository configuration usable as an explicit bootstrap/migration source.",
      "scope": [
        "Define a versioned trusted policy representation under Greenlight state",
        "Add an explicit command to initialize or update policy from repository config",
        "Load authoritative run policy from the trusted snapshot",
        "Record a deterministic policy digest",
        "Document migration for existing repositories"
      ],
      "non_goals": ["Cloud policy service", "Browser policy editor", "RBAC"],
      "acceptance": [
        "Changing .greenlight.toml on a feature branch does not silently change authoritative policy",
        "Missing trusted policy produces an actionable fail-closed error",
        "Policy updates are explicit and atomic",
        "Existing users have a documented one-command migration"
      ],
      "validation": ["uv run --extra dev pytest -q tests/test_config.py tests/test_cli.py tests/test_e2e.py", "uv run ruff check src/greenlight tests"]
    },
    {
      "id": "GL-003",
      "phase": "integrity",
      "title": "Make verification requirements explicit",
      "branch": "fix/required-verification",
      "pr_title": "fix(verify): fail closed on required checks",
      "depends_on": ["GL-002"],
      "intent": "Prevent unavailable verification from appearing green. Model required, optional, passed, failed, and unavailable verification explicitly and block forwarding whenever an applicable required check cannot run.",
      "scope": [
        "Add required/optional verification semantics",
        "Represent unavailable separately from skipped",
        "Block forwarding on unavailable required verification",
        "Preserve optional skip behavior with explicit output"
      ],
      "non_goals": ["Changing test auto-detection broadly", "Frontend UI", "CI provider integration"],
      "acceptance": [
        "An unavailable required backend or frontend check fails the gate",
        "An optional unavailable check is visibly non-green but does not block",
        "The event and CLI output distinguish skipped from unavailable",
        "Existing configured passing checks still pass"
      ],
      "validation": ["uv run --extra dev pytest -q tests/test_pipeline_ordering.py tests/test_e2e.py tests/test_config.py", "uv run ruff check src/greenlight tests"]
    },
    {
      "id": "GL-004",
      "phase": "integrity",
      "title": "Separate gate, forwarding, PR, and CI outcomes",
      "branch": "refactor/typed-pipeline-outcomes",
      "pr_title": "refactor(pipeline): model delivery outcomes explicitly",
      "depends_on": ["GL-003"],
      "intent": "Make Greenlight report truthful lifecycle outcomes. Distinguish local gates passing from candidate forwarding, PR delivery, and post-forward CI instead of collapsing them into one passed boolean.",
      "scope": [
        "Introduce typed pipeline and delivery outcomes",
        "Report PR creation failure separately",
        "Label current CI monitoring as post-forward observation",
        "Preserve CLI exit-code compatibility where possible",
        "Update renderers and tests"
      ],
      "non_goals": ["Provider-side required checks", "Changing GitHub merge policy", "UI implementation"],
      "acceptance": [
        "The result identifies whether any remote ref was updated",
        "PR delivery failure cannot be displayed as an all-green delivery",
        "Post-forward CI failure is never described as preventing the already-completed forward",
        "Legacy consumers receive a documented compatibility mapping"
      ],
      "validation": ["uv run --extra dev pytest -q tests/test_pipeline_ordering.py tests/test_ci.py tests/test_render.py", "npm test", "npm run typecheck"]
    },
    {
      "id": "GL-005",
      "phase": "integrity",
      "title": "Version and isolate run event journals",
      "branch": "feat/versioned-run-events",
      "pr_title": "feat(events): add versioned per-run journals",
      "depends_on": ["GL-004"],
      "intent": "Turn the existing telemetry stream into a replayable, concurrency-safe run record without moving pipeline authority out of Python. Give every run and event stable identity while preserving watch and review-log compatibility.",
      "scope": [
        "Add schema version, run ID, event ID, and monotonic sequence",
        "Record repository, base/head SHA, intent hash, and policy digest",
        "Write one append-only journal per run",
        "Add explicit stage lifecycle and terminal events",
        "Keep a compatibility pointer for the latest run",
        "Update Python and TypeScript reducers"
      ],
      "non_goals": ["Control commands in event files", "Hosted event service", "User identity beyond local actor metadata"],
      "acceptance": [
        "Concurrent runs cannot truncate or interleave each other's journals",
        "Every run including no-change and cancellation has one terminal event",
        "Unknown additive fields are tolerated and unknown major versions fail visibly",
        "watch, review-log, and the Pi card still consume legacy logs"
      ],
      "validation": ["uv run --extra dev pytest -q tests/test_events.py tests/test_render.py tests/test_review_log.py tests/test_watch_staleness.py", "npm test", "npm run typecheck"]
    },
    {
      "id": "GL-006",
      "phase": "integrity",
      "title": "Isolate run refs and forward with compare-and-swap",
      "branch": "fix/isolated-run-refs",
      "pr_title": "fix(git): isolate runs and guard forwarding",
      "depends_on": ["GL-005"],
      "intent": "Ensure a run validates and forwards exactly the candidate it records. Stop concurrent runs from moving shared branch refs and reject stale forwarding when the source or remote branch changed.",
      "scope": [
        "Create immutable per-run refs/worktree branches",
        "Serialize or safely isolate repository mutations",
        "Record expected local and remote heads",
        "Use force-with-lease or equivalent compare-and-swap forwarding",
        "Add stale and concurrent run tests"
      ],
      "non_goals": ["Distributed locking", "Multi-host runners", "UI controls"],
      "acceptance": [
        "Two runs for one repository cannot validate or forward the other's HEAD",
        "A changed remote destination rejects forwarding without overwrite",
        "The source branch is not moved by checkout",
        "Cleanup removes run refs and worktrees on terminal outcomes"
      ],
      "validation": ["uv run --extra dev pytest -q tests/test_gate.py tests/test_cleanup.py tests/test_e2e.py tests/test_pipeline_ordering.py", "uv run ruff check src/greenlight tests"]
    },
    {
      "id": "GL-007",
      "phase": "integrity",
      "title": "Externalize and redact verification evidence",
      "branch": "feat/external-run-evidence",
      "pr_title": "feat(evidence): store redacted run artifacts externally",
      "depends_on": ["GL-006"],
      "intent": "Keep verification evidence from mutating the authorized source commit and prevent obvious credentials from being exposed through future UI surfaces. Store bounded, redacted artifacts with the run record and link them into delivery summaries.",
      "scope": [
        "Move test logs and screenshots to per-run state storage",
        "Add bounded retention and size limits",
        "Redact common secret patterns before persistence or display",
        "Expose artifact metadata through events and PR summaries",
        "Document compatibility for repositories expecting committed evidence"
      ],
      "non_goals": ["Perfect secret detection", "Remote artifact storage", "Changing source code during verification"],
      "acceptance": [
        "Verification does not change the candidate HEAD",
        "Artifacts are attributable to run ID and content digest",
        "Configured size and retention limits are enforced",
        "Representative tokens and credentials are redacted in persisted text"
      ],
      "validation": ["uv run --extra dev pytest -q tests/test_e2e.py tests/test_events.py tests/test_cleanup.py", "uv run ruff check src/greenlight tests"]
    },
    {
      "id": "GL-008",
      "phase": "console",
      "title": "Add a loopback repository and run API",
      "branch": "feat/console-api",
      "pr_title": "feat(console): add loopback control API",
      "depends_on": ["GL-007"],
      "intent": "Create the minimal local service boundary for the Gate Console without reimplementing Greenlight. Register trusted repositories by opaque ID, expose handoff metadata, and supervise existing CLI runs on loopback only.",
      "scope": [
        "Add an optional console dependency group",
        "Bind only to 127.0.0.1 with a random launch token",
        "Implement canonical repository registration and health",
        "Expose branch, diff, intent, policy, and run launch APIs",
        "Reject browser-supplied executable and internal paths",
        "Own subprocess groups independently of browser connections"
      ],
      "non_goals": ["Remote access", "Authentication accounts", "Finding remediation", "Pi sessions"],
      "acceptance": [
        "The service cannot bind publicly",
        "Origin and CSRF checks reject unauthorized requests",
        "Only registered canonical repositories can be addressed",
        "Refreshing or closing a browser does not cancel a run",
        "A service restart marks active runs abandoned rather than fabricating completion"
      ],
      "validation": ["uv run --extra dev pytest -q tests/test_cli.py tests/test_events.py", "uv run ruff check src/greenlight tests"]
    },
    {
      "id": "GL-009",
      "phase": "console",
      "title": "Add replayable Console event streaming",
      "branch": "feat/console-sse",
      "pr_title": "feat(console): stream replayable run events",
      "depends_on": ["GL-008"],
      "intent": "Let a browser reconnect to running and completed Greenlight runs reliably. Add SSE replay over the versioned journals, bounded diagnostics, and explicit cancellation semantics without making the web service gate authority.",
      "scope": [
        "Expose SSE with Last-Event-ID replay",
        "Add run list/detail and artifact metadata APIs",
        "Apply output redaction and response size bounds",
        "Cancel process groups only before forwarding starts",
        "Reconcile terminal, abandoned, and cancellation states"
      ],
      "non_goals": ["WebSockets", "Resuming a crashed pipeline", "Review decision commands"],
      "acceptance": [
        "Reconnect replays each event once in sequence",
        "Slow consumers cannot cause unbounded memory growth",
        "Cancellation after forwarding begins is rejected with the observed delivery state",
        "The API never converts supervisor cancellation into a gate pass or failure"
      ],
      "validation": ["uv run --extra dev pytest -q tests/test_events.py tests/test_watch_staleness.py", "uv run ruff check src/greenlight tests"]
    },
    {
      "id": "GL-010",
      "phase": "console",
      "title": "Build the Console project and handoff screens",
      "branch": "feat/console-handoff-ui",
      "pr_title": "feat(console): add project and handoff UI",
      "depends_on": ["GL-009"],
      "intent": "Provide a focused browser entry point for selecting a registered repository and deliberately launching Greenlight on an exact commit. Do not recreate an editor or generic coding-agent interface.",
      "scope": [
        "Create the React application and typed API client",
        "Implement project health and recent runs",
        "Implement branch, exact SHA, diff summary, policy, and intent handoff",
        "Block dirty or invalid handoffs",
        "Add accessible setup, loading, and error states"
      ],
      "non_goals": ["Editor", "Terminal", "File explorer", "Review intervention", "Pi chat"],
      "acceptance": [
        "A user can select a registered project and authorize a run on a displayed exact SHA",
        "Intent is editable before launch and frozen afterward",
        "Setup failures have actionable recovery text",
        "Keyboard and screen-reader paths cover all primary actions"
      ],
      "validation": ["npm test", "npm run typecheck", "npm run build"]
    },
    {
      "id": "GL-011",
      "phase": "console",
      "title": "Build the run timeline and outcome UI",
      "branch": "feat/console-run-ui",
      "pr_title": "feat(console): visualize runs and findings",
      "depends_on": ["GL-010"],
      "intent": "Make Greenlight's transitions, findings, evidence, and delivery status understandable without terminal fallback. Preserve every review round and distinguish gate truth from forwarding, PR, and CI outcomes.",
      "scope": [
        "Implement the append-only run timeline",
        "Preserve reviewer and finding history by stable ID and round",
        "Add finding details with diff context",
        "Render evidence and bounded diagnostics",
        "Implement passed, failed, cancelled, abandoned, forwarded, PR-failed, and post-forward-CI states",
        "Reconnect automatically through SSE"
      ],
      "non_goals": ["Finding waivers", "Remediation controls", "Policy editing"],
      "acceptance": [
        "Every versioned lifecycle event has an understandable visual state",
        "Prior-round findings are never overwritten",
        "A refresh during a run restores the same timeline",
        "The UI never says nothing was forwarded when forwarding occurred"
      ],
      "validation": ["npm test", "npm run typecheck", "npm run build"]
    },
    {
      "id": "GL-012",
      "phase": "intervention",
      "title": "Pause safely for remediation decisions",
      "branch": "feat/review-decision-checkpoints",
      "pr_title": "feat(review): add safe decision checkpoints",
      "depends_on": ["GL-011"],
      "intent": "Add an optional interactive review mode where Greenlight pauses after a complete blocking round and accepts authenticated remediation selections without allowing users to redefine gate truth. Keep current autonomous CLI behavior compatible.",
      "scope": [
        "Define a versioned local control protocol separate from event journals",
        "Emit decision-required events with immutable finding IDs and basis",
        "Accept fix-selected, fix-all, or stop",
        "Reject stale commands by run nonce, HEAD, intent hash, and policy digest",
        "Mark waiting runs abandoned after supervisor restart",
        "Preserve noninteractive autonomous mode"
      ],
      "non_goals": ["Dismiss blocker", "Accept risk", "Unchanged-basis dispute rerun", "Mid-run intent edits"],
      "acceptance": [
        "Unselected blockers remain blocking",
        "No command can mark a finding resolved",
        "A stale or replayed command has no effect",
        "Stopping cannot forward",
        "Existing CLI runs retain documented autonomous behavior"
      ],
      "validation": ["uv run --extra dev pytest -q tests/test_review_fail_closed.py tests/test_review_events.py tests/test_events.py", "uv run ruff check src/greenlight tests"]
    },
    {
      "id": "GL-013",
      "phase": "intervention",
      "title": "Implement selected remediation and full re-review",
      "branch": "feat/selected-remediation",
      "pr_title": "feat(review): remediate selected findings",
      "depends_on": ["GL-012"],
      "intent": "Let users choose which findings the fix agent should address while ensuring every configured reviewer re-evaluates the changed candidate and every remaining blocker still fails closed.",
      "scope": [
        "Pass only selected immutable findings and optional instructions to the fix agent",
        "Record pre/post fix HEAD and commit",
        "Rerun all reviewers after every changed HEAD",
        "Track reproduced, resolved, and new observations without equating absence in a partial round with resolution",
        "Expose remediation controls through the Console"
      ],
      "non_goals": ["Waivers", "Partial reviewer reruns", "Repeated reruns without a changed HEAD"],
      "acceptance": [
        "Only selected findings influence the fix prompt",
        "Every changed HEAD receives a complete reviewer round",
        "A malformed, inconclusive, reproduced, new, or unselected blocker prevents progress",
        "The Console displays the resulting delta and complete round history"
      ],
      "validation": ["uv run --extra dev pytest -q tests/test_review_fail_closed.py tests/test_review_events.py tests/test_review_parallel.py", "npm test", "npm run typecheck"]
    },
    {
      "id": "GL-014",
      "phase": "intervention",
      "title": "Authorize the exact final candidate before forwarding",
      "branch": "feat/final-head-authorization",
      "pr_title": "feat(gate): require final candidate authorization",
      "depends_on": ["GL-013"],
      "intent": "Bind human forwarding authorization to the exact post-remediation candidate and its complete clean review/verification basis. Show the original-to-final delta and use compare-and-swap forwarding so authorization cannot drift.",
      "scope": [
        "Emit final-authorization-required after clean review and verification",
        "Display original/final SHA and complete delta",
        "Bind authorization to final HEAD, intent, policy, and verification digests",
        "Reject mutation or remote drift after authorization",
        "Forward only through the existing guarded Git path"
      ],
      "non_goals": ["Ship anyway", "Bulk standing approval", "Post-forward cancellation"],
      "acceptance": [
        "No interactive run forwards before exact final authorization",
        "Any basis change invalidates authorization",
        "Remote drift fails without overwriting",
        "The audit record identifies actor, candidate, and decisive basis"
      ],
      "validation": ["uv run --extra dev pytest -q tests/test_gate.py tests/test_pipeline_ordering.py tests/test_events.py", "npm test", "npm run typecheck"]
    },
    {
      "id": "GL-015",
      "phase": "workbench",
      "title": "Add a supervised Pi SDK worker",
      "branch": "feat/pi-session-worker",
      "pr_title": "feat(workbench): add supervised Pi sessions",
      "depends_on": ["GL-014"],
      "intent": "Add Pi as the coding engine behind a narrow local worker interface. Persist coding sessions, normalize typed events, and keep browser tabs from owning agent processes while preserving Greenlight as a separate independent gate.",
      "scope": [
        "Create a Node worker using AgentSessionRuntime",
        "Expose create, prompt, steer, follow-up, abort, and state operations to the loopback service",
        "Persist sessions and resubscribe after runtime replacement",
        "Normalize message and tool events with replay cursors",
        "Redact and bound tool output",
        "Add process supervision and crash reconciliation"
      ],
      "non_goals": ["Remote runner", "Multiple simultaneous writers per repository", "Browser access to raw session paths"],
      "acceptance": [
        "A session survives browser reconnect and service restart with completed messages intact",
        "Abort, steer, and follow-up have deterministic visible states",
        "Credentials and thinking blocks are not exposed by default",
        "The worker cannot address an unregistered repository"
      ],
      "validation": ["npm test", "npm run typecheck", "npm run build"]
    },
    {
      "id": "GL-016",
      "phase": "workbench",
      "title": "Build the Pi coding Workbench",
      "branch": "feat/pi-workbench-ui",
      "pr_title": "feat(workbench): add prompt-to-handoff flow",
      "depends_on": ["GL-015"],
      "intent": "Complete the local product loop from coding prompt to deliberate Greenlight handoff without building a browser IDE. Stream Pi conversation and summarized activity, then freeze a committed exact candidate and agent-authored intent for the existing Gate Console.",
      "scope": [
        "Add task creation and prompt composer",
        "Render Pi responses and collapsed tool activity",
        "Show changed files, diff stats, test and commit status",
        "Support stop, steer, follow-up, and open-in-external-editor",
        "Have Pi commit on a feature branch and author structured intent",
        "Transition into the existing exact-SHA handoff"
      ],
      "non_goals": ["Monaco editor", "Embedded terminal", "File explorer", "Model marketplace", "Cloud collaboration"],
      "acceptance": [
        "A user can go from prompt to a committed handoff and final Greenlight outcome",
        "Greenlight never starts automatically when Pi finishes",
        "The handoff displays the exact candidate and editable pre-run intent",
        "Agent failure leaves recoverable session and repository state"
      ],
      "validation": ["npm test", "npm run typecheck", "npm run build"]
    },
    {
      "id": "GL-017",
      "phase": "hardening",
      "title": "Harden and document the autonomous product flow",
      "branch": "test/product-flow-hardening",
      "pr_title": "test(product): harden prompt-to-gate workflow",
      "depends_on": ["GL-016"],
      "intent": "Close the product roadmap with realistic end-to-end validation, failure recovery, accessibility, security documentation, and operator guidance. Prove the prompt-to-handoff-to-gate flow without expanding into hosted execution.",
      "scope": [
        "Add browser smoke tests for primary and failed flows",
        "Test crash, reconnect, stale command, cancellation, and restarts",
        "Audit loopback security, retention, and redaction",
        "Document installation, migration, troubleshooting, and architecture",
        "Update changelog and screenshots"
      ],
      "non_goals": ["Hosted execution", "SSO/RBAC", "Billing", "Deployment"],
      "acceptance": [
        "The complete local journey passes in automated smoke coverage",
        "Every known terminal and recovery state has user-facing guidance",
        "Security documentation states the no-sandbox boundary and recommends contained unattended execution",
        "Python tests, Ruff, frontend tests, typecheck, build, and accessibility checks pass"
      ],
      "validation": ["uv run --extra dev pytest -q", "uv run ruff check .", "npm test", "npm run typecheck", "npm run build"]
    }
  ]
}
